{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "test": {
      "dependsOn": [],
      "inputs": ["src/**/*.test.ts"]
    },
    "lint": {
      "outputs": []
    },
    "cleanup": {
      "outputs": []
    },
    "deploy": {
      "dependsOn": ["build", "test", "lint"],
      "outputs": []
    },
    "@tadeas/webapp#dev": {
      "outputs": [".next/**"]
    },
    "@tadeas/functions#dev": {
      "outputs": ["dist/**"]
    }
  }
}
