{
  "name": "@tadeas/firestore-converters",
  "version": "1.0.0",
  "author": "Oliver Sosa",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "cleanup": "rm -rf node_modules/; rm -rf dist/; rm -rf .turbo/",
    "dev": "tsup src/index.ts --watch --dts --sourcemap",
    "build": "tsup src/index.ts --dts --sourcemap"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "tsup": "^6.5.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
